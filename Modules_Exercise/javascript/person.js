/**
 * @description this function will provide more personal information
 * @param{*} first is person's first name
 * @param{*} last is person's last name
 * @param{*} age is person's age
 * @returns
 */

export var Person = (first, last, age) => {
    return "My name is " + first + " " + last + " and I am " + age + " years old."
}