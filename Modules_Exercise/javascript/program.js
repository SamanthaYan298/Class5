/**
 * @desc this function provides more information about this person
 * @param {*} department shows the person's object
 * @param {*} degree shows their program
 * @returns 
 */

export var Program = (department, degree) => {
    return `I am studying the ${degree} in the  ${department}.` 
};
